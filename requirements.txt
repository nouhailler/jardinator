# Jardinator — Dépendances Python (version desktop PyQt6)
# Installation : pip install -r requirements.txt

# Interface graphique
PyQt6>=6.7.0

# Requêtes HTTP (téléchargement images Wikimedia Commons)
requests>=2.31.0

# --- Dépendances stdlib (incluses avec Python 3.10+, pas besoin d'installer) ---
# sqlite3          — base de données locale
# json             — lecture des fichiers de données
# logging          — journalisation
# pathlib          — gestion des chemins
# typing           — annotations de types
# datetime         — manipulation de dates
